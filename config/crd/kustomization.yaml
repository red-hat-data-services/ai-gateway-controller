apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Development/test packaging only. The platform/operator owns installation of
# these CRDs; config/self/default does not include this kustomization.

resources:
  - bases/inference.opendatahub.io_externalmodels.yaml
  - bases/inference.opendatahub.io_externalproviders.yaml
